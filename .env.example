# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# IMPORTANT: Service Role Key - SERVER ONLY!
# NEVER expose this to the client
# Only use in server-side code/edge functions
VITE_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Admin Secret - SERVER ONLY!
# Used to protect admin endpoints (sync, rebuild, doctor)
# Generate with: openssl rand -hex 32
VITE_ADMIN_SYNC_SECRET=your-admin-secret-here

# Cron Secret - SERVER ONLY!
# Used to protect scheduled/cron endpoints
# Generate with: openssl rand -hex 32
VITE_CRON_SECRET=your-cron-secret-here

# Platform API Keys (optional)
# These are for data sync features
VITE_FANTASYPROS_API_KEY=your-fantasypros-key
VITE_SLEEPER_API_KEY=your-sleeper-key

# Environment
VITE_ENVIRONMENT=development

# Rate Limiting (optional)
# Set to 'database' to use Supabase instead of in-memory
VITE_RATE_LIMIT_STORAGE=memory
